#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    int n;
    cout << "Enter a number : ";
    cin >> n;
    sqrt(n);
    cout << "The square root of the given number : ";
    cout << sqrt(n);
}